<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.jaram.ds" >

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".Intro"
            android:label="@string/app_name" >
            <intent-filter>

            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity

            android:name=".admin.OrderManagerMain"
            android:label="OrderManager">
        </activity>

        <activity

            android:name=".order.OrderManager"
            android:label="orderManager">
        </activity>

        <activity
            android:name=".admin.MenuManagementMain"
            android:label="MenuManage"
            >
        </activity>

        <activity
            android:name=".admin.view.ManagementIntro"
            android:label="ManagementIntro">
        </activity>

        <activity
            android:name=".admin.CategoryMenagement"
            android:label="CategoryMenagement">
        </activity>

        <activity
            android:name=".admin.Statistic2"
            android:label="newStatistic">
        </activity>


        <meta-data android:name="DATABASE" android:value="test.db" />
        <meta-data android:name="VERSION" android:value="10" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="org.jaram.ds" />
    </application>

</manifest>